import { PreffixeProvPipe } from './preffixe-prov.pipe';

describe('PreffixeProvPipe', () => {
  it('create an instance', () => {
    const pipe = new PreffixeProvPipe();
    expect(pipe).toBeTruthy();
  });
});
